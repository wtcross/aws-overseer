overseer_name: "{{ overseer_name }}"
overseer_aws_region: "{{ overseer_aws_region }}"
overseer_aws_access_key: "{{ overseer_aws_access_key }}"
overseer_aws_secret_key: "{{ overseer_aws_secret_key }}"
